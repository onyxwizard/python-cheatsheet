# Identifiers are names that identify variables, functions, modules, classes, and other objects in Python.
# Python identifiers are case-sensitive. 
# For example, the counter and Counter are different identifiers.

Count = 0
count = 1

print(f'Count = {Count} is different from count = {count}')
