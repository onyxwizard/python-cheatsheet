def main(a,b,c):
    # a long statement can span multiple lines by using the backslash (\) 
    print(a==True,b==True,c ==True)
    if (a==True) and (b==True) and \
        (c==True):
            print("a long statement can span multiple lines by using the backslash (\) ")

main(1,1,0)